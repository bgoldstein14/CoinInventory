# SQL Server connection (use DB_PORT for explicit port; do NOT use "server,port" comma syntax)
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=CoinInventory

# SQL Server authentication (leave blank for Windows / Integrated auth)
DB_USER=
DB_PASSWORD=

# Server port
PORT=3000
